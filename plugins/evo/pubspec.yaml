name: evo_protocol
description: "A new Flutter plugin project."
version: 0.0.1
homepage:
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  mt_interface:
    path: ../multi_transport/mt_interface