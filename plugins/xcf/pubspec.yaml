name: xcf_protocol
description: "A new Flutter plugin project."
version: 0.0.1
homepage: "https://www.becker-antriebe.com"
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  hex: ^0.2.0
  collection: ^1.17.1
  version: ^3.0.2
  mt_interface:
    path: ../multi_transport/mt_interface
